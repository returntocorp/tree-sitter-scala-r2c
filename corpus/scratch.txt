==========================
Scratch
==========================
object foo {
	for {
	  x <- Nil
	} { x + 1 }
}
-----
(compilation_unit (object_definition (identifier) (template_body (for_expression (enumerators (generator (identifier) (identifier))) (block
(infix_expression (identifier) (identifier) (number)))))))

